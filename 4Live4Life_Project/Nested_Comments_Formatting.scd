

// SuperCollider

/*
The whole text should be formatted in comments even with nested comments...
The whole text is highlighted correctly in grey within SuperCollider

if ( thisProcess.platform.name == \linux /*thisProcess.platform.isKindOf(LinuxPlatform)*/ );

if ( thisProcess.platform.name == \linux /*thisProcess.platform.isKindOf(LinuxPlatform)*/ );

MIDIClient.init;

*/

